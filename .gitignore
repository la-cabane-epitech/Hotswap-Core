# Build
build/
cmake-build-*/

# Binaires & artefacts
*.o
*.so
*.dylib
*.dll
a.out
# Anchored to the root: without the leading "/", the "FileWatcher" pattern also
# ignores the src/filewatcher/ directory (git has core.ignorecase=true on macOS).
/main
/FileWatcher

# Pipeline runtime artifacts: active library, candidate and logs.
/.hotswap/

# Conan
conan_toolchain.cmake
conanbuild*
conanrun*
CMakeUserPresets.json

# macOS
.DS_Store